# 变更记录

## Unreleased

- 统一抖音下载为 yt-dlp 优先、浏览器自动回退、双失败人工接管，复用配置保存的加密登录。
- 分析默认 720p；主动下载未选画质时要求选择，支持 720p、1080p 和最高可用画质。
- 增加完整性校验、脱敏路线报告和临时凭据清理，专用 Python 环境锁定 yt-dlp。

## 1.1.0 — 架构整理，未发布

- 保留公开 CLI 路径，将业务模块移至 src，测试统一至 tests。
- ASR/Omni 共用执行生命周期，本地模型任务不依赖抖音登录。
- TaskBudget 增加严格状态迁移和归档，区分已知费用与未知费用估算。
- 模型调用前预留输出，每步保存结果检查点；失败不覆盖已完成结果。
- 结果下载不携带 API Key，禁用重定向；未知模型须明确配置价格。
- 分镜结果增加严格解析入口、版本及完整性校验。
- 配置服务与 HTTP 分离，设置采用写锁和版本校验。
- 统一离线测试、Windows CI、依赖哈希锁定及可追溯发布包。

兼容性：Node 最低版本统一为 22；内部模块路径迁至 src，旧 CLI 参数保留且不再静默忽略非法参数；Filetrans 失败不自动切换 endpoint 重发。归档 Task ID 不可复用。
